class camera:
  def _init_(self,name):
    self.name=name
  def capture(self):
    print("print from camera class")
class sphone(camera):
  def __init__(self,name,resolution):
    super().__init__(name)
    self.resolution=resolution
  def capture(self):
    print("from capture smartphone")


class DSLR(camera):
  def __init__(self,name,resolution):
    super().__init__(name)
    self.resolution=resolution
  def camera(self):
      print("dslr is here")

class DROne(camera):
  def __init__(self,name,resolation):
    super().__init__(name)
    self.resolation=resolation
  def camera(self):
li=sphone("ipone",300)
dslr=DSLR("SONY",44444)
drone=DROne("Pakhi",435)

li.capture()
dslr.capture()
drone.capture()
    print("drone capture")

